services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: best-cover-letter-app
    restart: always
    ports:
      - ${SERVER_PORT}:${SERVER_PORT}
    env_file:
      - .env
